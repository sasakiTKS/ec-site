</br><div class="container">
<div class="row">
  <div class="col-xs-10 col-xs-offset-1"><h2 class="head-line title_h2 text-left"><span class="bg-light" >注文履歴一覧</span></h2>
      <div class="col-xs-10 col-xs-offset-1">
      <!--<div class="col-xs-10 col-xs-offset-1"style="margin-top: 50px;">-->
        <table class="table table-hover table-bordered table-condensed ">
          <thead>
            <tr class="table-head bg-light">
              <th scope="col">購入日時</th>
              <th scope="col">購入者</th>
              <th scope="col">注文個数</th>
              <th scope="col">注文ステータス</th>
            </tr>
          </thead>
              <tbody>
        <% @orders.each do |order| %>
          <tr>
            <td><%= link_to order.created_at.strftime("%Y-%m-%d %H:%M:%S"), admin_order_path(order) %></td>
            <td><%= order.customer.last_name %>
              <%= order.customer.first_name %>
            </td>
            <td><%= order.order_details.sum(:amount) %></td>
            <td><%= order.status %></td>
          </tr>
        <% end %>
      </tbody>
        </table>
      </div>
    </div>
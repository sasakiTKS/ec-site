</br><div class="container">
  <div class="row">
    <div class="col-md-3">
      <table class="genre-table table table-bordered table-hover">
  <thead class="genre-thead">
    <tr>
      <th class="active">ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to @items_path do %>
            <%= genre.name %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
 </table>
    </div>
    <div class="col-md-9">
      <div class="top-box">
        <p class="top-text">
          ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。<br>
        </p>
      </div>

      <div class="recommend-title">
        <h3>新着商品</h3>
      </div>
      <div class="recommend-container ">
        <div class="row recommend-item">
         <div class="col-md-10 mt-4">
         <div class="d-flex flex-inline">
         <% @items.last(4).each do |item| %>
          <span class="ml-5">
          <div class="item-container">
          <%= link_to public_item_path(item.id) do %>
          <% if item.image.attached? %>
            <%= image_tag item.image, size: "200x175" %>
            <% else %>
            <%= image_tag 'no_image', size: "200x175" %>
            <% end %>
          <% end %>
          <p>
          <%= link_to public_item_path(item.id), class: "btn-link text-dark" do %>
          <%= item.name %>
          <% end %>
          </p>
          <% if item.is_active == true %>
          <p>¥<%= item.price.to_s(:delimited) %> </p>
          <% elsif item.is_active == false %>
          <p>売り切れ中</p>
          <% end %>
          </div>
          </span>
         <% end %>
         </div>
         <%= link_to "全ての商品を見る>", public_items_path, style: "text-decoration:underline;" %>
         </div>
        </div>
        </div>
    </div>
  </div>
</div>
<!--%= link_to destroy_customer_session_path, method: :delete, class:"nav-link text-center" do %--><!--簡易ログアウト--><!--% end %-->